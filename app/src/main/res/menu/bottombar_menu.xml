<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/bottombaritem_home"
        android:icon="@drawable/ic_round_home"
        android:title="@string/home"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/bottombaritem_receipt"
        android:icon="@drawable/ic_round_receipt"
        android:title="@string/order"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/bottombaritem_menu"
        android:icon="@drawable/ic_round_restaurant_menu"
        android:title="@string/menu"
        app:showAsAction="ifRoom"/>

</menu>